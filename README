1. Run make in property-doc

2. run the property-doc perl script on java packages and subpackages

The following example run was done inside property_db/java-api:

../property-doc/bin/propertydocs.pl java/io/* java/net/* -package java.lang java.util -subpackages java.lang.annotation java.lang.instrument java.lang.management java.lang.ref java.lang.reflect java.util.regex java.util.concurrent java.util.logging java.util.zip java.util.prefs java.util.spi java.util.concurrent.locks java.util.concurrent.atomic -d $OUTPATH

NOTE: don't forget to set an OUTPATH!
This can be any directory you want. Just keep track of it for the eventual sftp to the server!

The preferred OUTPATH=/property_db/property-doc/curr_web_files, as it's under version control already.

1. Then ftp the curr_web_files directory to RV's site (in the /var/www/monitor/annotated-java-8 directory)



____ Working Notes
-- problem with java.util.jar
JarEntry_GetCertificatesOnce

